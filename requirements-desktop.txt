# Additional dependencies for running/building DocForge as a desktop app.
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-desktop.txt

pywebview==5.3.2
pyinstaller==6.10.0
